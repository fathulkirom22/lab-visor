
{% extends "base.jinja" %}
{% import "sys/chart.jinja" as cart %}
{% import "sys/text.jinja" as text %}

{% block title %}{{title}}{% endblock %}

{% block content %}
    <div class="container">

        <div class="pb-4">
            <div class="row mb-5">
                <div class="col-md-6 mb-5 mb-md-0">
                    <div id="systemInfo">
                        <h4>
                            <i class="bi bi-info-circle"></i>
                            System Info
                        </h4>
                        {{text.base('Uptime', '/api/sys/uptime', 'text', 15)}}
                        {{text.base('Operating System', '/api/docker/info', 'OperatingSystem')}}
                        {{text.base('Architecture', '/api/docker/info', 'Architecture')}}
                        {{text.base('CPU Cores', '/api/docker/info', 'NCPU')}}
                        {{text.base('RAM', '/api/docker/info', 'MemTotal')}}
                    </div>
                </div>
                <div class="col-md-4">
                    <div id="diskInfo">
                        <h4>
                            <i class="bi bi-hdd"></i>
                            Disk
                        </h4>
                        {{ cart.disk('disk') }}
                    </div>
                </div>
            </div>

            {{ cart.realtimeResourceUsage('realtimeResourceUsage') }}

            <div class="row align-items-start">
                <div class="col">
                    <h4><i class="bi bi-bar-chart-steps"></i> Resource Tracker</h4>
                    {{ cart.resourceUsage('resource') }}
                </div>
            </div>
        </div>
    </div>

    <script type="module">
        
    </script>
{% endblock %}
